[project]
name = "signalwatch"
version = "0.2.0"
description = "Reliable, extensible event monitoring and ntfy delivery service"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
signalwatch = "signalwatch.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

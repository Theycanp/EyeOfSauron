# EOS 改进路线图

这份路线图记录当前生产观察、目标结构、实施顺序和验收标准。它是工程
计划，不代表所有条目已经实现；已完成的条目必须在 `CHANGELOG.md` 和
对应专题文档中留下事实记录。

## 当前基线

天气模块正在 `feat/local-weather` 开发，尚未部署。已实现范围、已探测接口和
后续验收条件见 `WEATHER.md`：首版采用 Open-Meteo 日预报与 QWeather 临近雨雪、
官方预警分工；小时预报互证、多模型校准、完整降水生命周期、本地准确率学习和
多地点订阅均不得误称已实现或已启用。

- 采集、事件投影、日报、通知和 React 管理端已经可以独立运行。
- SQLite 是当前持久化适配器，业务代码通过 repository/port 访问数据。
- 日报有算法版兜底、AI 重试和连续失败升级机制。
- 事件池已经成为日报和日常事件页面的共同数据源。
- 9 月 16 日缺报的重复键错误已由 v0.19.0 修复；9 月 17 日 00:50–01:19 UTC
  的报错早于修复部署，不能当作 v0.20.0 回归。最新一次核查的 9 月 17 日
  日报已是正常发布的 API 版。
- 需改进的正文样本：欧盟委员会 11 条空正文、IAEA 4 条 HTTP 403。正文任务
  失败不阻断 RSS，摘要已经保留，不应为此新建另一套采集流程。
- 大量单来源事件和泛化标题值得抽样评估，但历史回填、不同日期栏目和本地
  天气报道都会影响分母，不能只凭总量认定聚类有错。
- ProviderRegistry、仓储/通知/AI 端口、长期来源权重、主备模型、Prompt 管理、
  登录 Cookie/CSRF/RBAC、日常事件页及动态日报数已具备；本轮扩展现有边界。

## 交付追踪

状态仅描述代码事实，提交、CI 和实际部署后再补对应证据。按批次推进所有
约定阶段；完成一个批次后继续下一项，不将此表当作中途停止的理由。

| 批次 | 交付内容 | 当前状态 | 验收标准 |
| --- | --- | --- | --- |
| R1 | 日报证据保留、重试窗口、异常输出及崩溃恢复 | 已部署（v0.20.1） | 抖动下共五次尝试；非法输出仍发算法版；重启不延长期限；不重复通知 |
| R2 | 正文失败可视化、来源解析与退避 | 已部署（v0.21.0） | 摘要可读、403不刷屏、解析快照验证 |
| E1 | 事件质量指标、生命周期和受控管理 | 已部署（v0.21.0） | 不误合并；操作可审计；旧日报保留；时间窗稳定 |
| S1 | 来源端口审计、诊断和健康/长期质量隔离 | 已部署（v0.21.0） | 新适配器不改核心；统计有明确窗口/分母；停用可恢复 |
| D1 | 日报运行记录和管理操作 | 已部署（v0.21.0） | 管理端能看实际生成状态、尝试和错误，人工操作有权限/幂等保护 |
| U1 | 工作台筛选、关注及日报纳入/排除 | 已部署（v0.21.0） | 手机/桌面可用；用户选择和算法路由边界清晰 |
| O1 | 故障回放、安全与运维审计 | v0.22.5 隔离发布/恢复演练已部署；完整服务恢复计时仍待补 | 完整门禁、回滚准备、受影响生产链路验收 |

PostgreSQL、外部消息队列和通用跨语言 AI 聚类是条件性后续项，本轮做边界
与适用性评估，不在缺乏容量证据时引入基础设施。外部 heartbeat、小模型、
传感器实际动作仍遵从用户既有的关闭选择。

这里的“已部署”只覆盖表中列出的工作台和可靠性能力。通用跨语言事件身份、
自动 Claim/Timeline、冲突事实持久化及统一事件更新通知仍是明确的后续边界，
详见 `EVENTS.md` 与 `EVENT_FACTS.md`；它们不能由现有的受控央行别名聚合结果推断为已经完成。

## 阶段 1：可靠性收紧（最高优先级）

### 1.1 日报发布状态机

后台目标显示：待生成、算法版已发布、AI 重试中、AI 已补发、重试结束和需
人工处理。由已有日报版本与 retry 状态推导，避免多份持久状态互相矛盾。

要求：

- 构建阶段和保存前都做确定性 `cluster_key` 去重；重复项合并证据，不丢弃
  一手、二手或社交报道。
- 错误按阶段分类：本地结构校验和证据读取在预留 AI 预算前完成；模型返回
  非法格式可在下一次生成改善，应允许重试。不得把所有 ValueError 当成永久错误。
- 每次尝试记录 provider、model、prompt 版本、错误类别、开始和结束时间。
- 算法版发布必须先于 AI 重试，重启不能丢失状态，重复执行必须幂等。

验收：同一批重复事件只产生一个日报项；结构性错误不会每分钟重复刷日志；
模型不可用时算法版仍然发布；成功重试只新增一个不可变 AI 版本。

### 1.2 正文抓取降级

保留现有正文级别和抓取任务状态，在读模型说明“有摘要/有全文”和“受限/解析
失败/待重试”；失败原因与正文可用程度分开。403 或解析失败时保留标题、摘要
和原文链接，并评估域名退避，不让全文失败阻断观察入库、事件聚类或日报。

验收：正文抓取失败的文章仍可在事件页和日报中阅读；重复 403 不会形成
高频重试；来源健康页能区分 RSS 成功与正文失败。

## 阶段 2：事件质量

### 2.1 聚类可解释性

保留 `Observation -> EventReport -> Event -> Digest/Alert` 四层关系。一手、
二手、社交报道在同一事件下平级保存，不覆盖原始事实。聚类必须使用实体、
主题、地区、时间和数字一致性，不能因泛化标题单独合并。

增加质量指标：单来源事件比例、重复标题比例、平均独立来源数、误合并和
未合并样本、冲突报道数量。为管理员提供合并、拆分、标记主事件和查看匹配
理由的审计入口。

### 2.2 事件生命周期

事件自动从 `active` 进入 `quiet`、`closed`，迟到的高匹配报道可以重新打开。
事件详情提供时间线、来源层级、冲突事实和通知记录。日报和日常事件页只
读取事件池，不维护第二套聚类结果。

## 阶段 3：信源和外部服务可插拔

复用现有 Collector/ProviderRegistry 端口，独立负责获取、游标、标准化、
错误分类和正文策略；审计任何仍需核心认识具体网站的缺口，不另建同义接口。

来源质量采用长期窗口和最低样本数：短期网络抖动只影响健康状态，长期低质量
才降低权重，人工覆盖必须审计。AI 和通知同样只通过 provider/transport 端口
接入，Prompt 采用版本化注册表。
现有质量默认至少 30 个有效样本、跨度 90 天、90 天半衰期，符合用户的数月
观察要求；不因为这次路线图改成短期 30 天调权。

验收：新增一个 RSS、官方列表或 API 来源只需实现适配器和目录条目；停用一个
来源不需要修改日报、事件或通知核心逻辑。

## 阶段 4：日报和管理端

- 日报数量继续由信息密度、重要度、新鲜度、来源独立性和主题覆盖动态决定，
  50 条只是硬上限，不是目标数量。
- AI 负责证据内归纳和表达，算法负责候选选择、通知路由和事实边界。
- 管理端增加日报尝试历史、来源诊断、事件聚类检查、权重解释、AI provider
  故障转移、Prompt 版本和通知渠道管理。
- 日常事件页面支持时间窗、重要度排序、来源层级、冲突状态、已读/关注和
  手动加入日报。

## 阶段 5：运维、测试和扩展

- 为 RSS/HTML 建立历史快照回放测试，为聚类建立属性测试和人工标注样本。
- 对模型超时、403、DNS 失败、ntfy 不可用、数据库锁和进程重启做故障注入。
- CI 必须执行迁移检查、完整测试、构建、部署后健康检查和回滚验证。
- 保持 SQLite 隔离、WAL、备份和恢复演练。只有在多实例并发写入、锁竞争或
  查询规模达到瓶颈时才迁移 PostgreSQL；队列先继续使用数据库持久任务表，
  通过 `JobQueue` 端口保留未来替换空间。

## 每个阶段的交付规则

每个实现 PR 必须同步更新受影响专题文档、`CHANGELOG.md`、测试和回滚说明。
生产部署前确认版本、schema、配置 revision、服务健康、来源状态、outbox 和
一条受控测试链路。未能完成的项目必须写明当前状态、阻塞原因、下一步和验收
条件，不能只留在聊天记录中。

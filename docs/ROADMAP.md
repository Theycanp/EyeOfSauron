# EOS 改进路线图

这份路线图记录当前生产观察、目标结构、实施顺序和验收标准。它是工程
计划，不代表所有条目已经实现；已完成的条目必须在 `CHANGELOG.md` 和
对应专题文档中留下事实记录。

## 当前基线

2026-09-26 审计基线为生产 v0.26.0、schema 27。天气首版随 v0.24.0
部署，随后加入详情、正午太阳高度角和独立天文通道健康状态；v0.26.0
增加地图选点、地点保存后及时刷新、雨雪/气温图、UV 显示，以及按每次发生
确认收到并在成功投递后计时重复的提醒。实际地点由运行交底记录，不由本文固化。
实际生产启用状态以发布交底和验收记录为准。
已实现范围、已探测接口和后续验收条件见 `WEATHER.md`：首版采用 Open-Meteo 日预报与 QWeather 临近雨雪、
官方预警分工；小时预报互证、多模型校准、完整降水生命周期、本地准确率学习和
多地点订阅均不得误称已实现或已启用。

- 采集、事件投影、日报、通知和 React 管理端已经可以独立运行。
- SQLite 是当前持久化适配器，业务代码通过 repository/port 访问数据。
- 日报有算法版兜底、AI 重试和连续失败升级机制。
- 事件池已经成为日报和日常事件页面的共同数据源。
- 9 月 16 日缺报的重复键错误已由 v0.19.0 修复；9 月 17 日 00:50–01:19 UTC
  的报错早于修复部署，不能当作 v0.20.0 回归。最新一次核查的 9 月 17 日
  日报已是正常发布的 API 版；2026-09-26 核查最近五期（9 月 22–26 日）均为
  已发布 API 版。
- 2026-09-26 正文快照：完成 2,818 条，死信 56 条，包括欧委会空正文 26 条、
  IAEA HTTP 403 18 条、日本央行不支持类型 9 条和空正文 1 条、发改委 404
  及英国政府 410 各 1 条。这是累计历史状态，不等于当前版本仍在报错。
  正文失败不阻断 RSS，摘要已经保留；受限内容不绕过访问或版权策略。
- 有限确定性事实投影已部署：本次快照的 25,147 个 Report 任务全部完成，
  无待处理、重试或死信任务，但仅提取 5 个自动 Claim。这表示历史任务已回填，
  不表示通用事实理解完成，也不能用“未提取到事实”判定消息无价值。
- 大量单来源事件和泛化标题值得抽样评估，但历史回填、不同日期栏目和本地
  天气报道都会影响分母，不能只凭总量认定聚类有错。
- ProviderRegistry、仓储/通知/AI 端口、长期来源权重、主备模型、Prompt 管理、
  登录 Cookie/CSRF/RBAC、日常事件页及动态日报数已具备；本轮扩展现有边界。

## 交付追踪

状态仅描述代码事实，提交、CI 和实际部署后再补对应证据。按批次推进所有
约定阶段；完成一个批次后继续下一项，不将此表当作中途停止的理由。

| 批次 | 交付内容 | 当前状态 | 验收标准 |
| --- | --- | --- | --- |
| R1 | 日报证据保留、重试窗口、异常输出及崩溃恢复 | 已部署（v0.20.1） | 抖动下共五次尝试；非法输出仍发算法版；重启不延长期限；不重复通知 |
| R2 | 正文失败可视化、来源解析与退避 | 已部署（v0.21.0） | 摘要可读、403不刷屏、解析快照验证 |
| E1 | 事件质量指标、生命周期和受控管理 | 已部署（v0.21.0） | 不误合并；操作可审计；旧日报保留；时间窗稳定 |
| S1 | 来源端口审计、诊断和健康/长期质量隔离 | 已部署（v0.21.0） | 新适配器不改核心；统计有明确窗口/分母；停用可恢复 |
| D1 | 日报运行记录和管理操作 | 已部署（v0.21.0） | 管理端能看实际生成状态、尝试和错误，人工操作有权限/幂等保护 |
| U1 | 工作台筛选、关注及日报纳入/排除 | 已部署（v0.21.0） | 手机/桌面可用；用户选择和算法路由边界清晰 |
| O1 | 故障回放、安全与运维审计 | 已部署；v0.25.0 补齐受控服务恢复计时，跨主机灾难恢复仍是独立演练 | 完整门禁、回滚准备、受影响生产链路验收 |

PostgreSQL、外部消息队列和通用跨语言 AI 聚类是条件性后续项，本轮做边界
与适用性评估，不在缺乏容量证据时引入基础设施。外部 heartbeat、小模型、
传感器实际动作仍遵从用户既有的关闭选择。

这里的“已部署”只覆盖表中列出的工作台和可靠性能力。有限自动 Claim/Timeline、
同一事实槽的争议持久化和审查过的发生身份索引已存在。通用跨语言身份、正文
事实抽取、明确更正、日报事实快照及统一事件更新通知仍是后续边界，详见
`EVENTS.md` 与 `EVENT_FACTS.md`；不能由受控央行别名聚合结果推断全面完成。

## 2026-09-26 日志审计与下一轮计划

本节把本轮日志修复与后续开发分开。列为“待做”的功能只是计划，不要求在
本轮日志修复中全部实现；提交、检查和生产验收之前不得改写为已部署。

### 本轮修复范围（v0.26.1；生产验收见运行交底）

- watchdog：正常轮询保持静默；只有可执行异常、恢复或修复失败输出状态。
  与发布/回滚共用互斥边界，发布期间不误重启服务，不能用静默掩盖真正故障。
  验收覆盖健康、退化、发布锁占用、命令失败和恢复路径，核查真实 timer 日志。
- HTTP 入口：正确处理 HEAD 与 favicon，避免浏览器常规探测制造无意义错误；
  不扩大匿名 API 访问范围。验收包括 HEAD 无响应正文、状态/头部正确、图标
  可加载，以及未登录业务 API 仍返回 401。
- 正文边界：区分历史死信、当前抓取失败和上游受限。IAEA 403 保留摘要与
  原文链接，不以代理绕限制；空正文需要真实样本回放后才调整解析规则。
  验收不得把正文受限变成 RSS 不健康，或无授权抓取商业媒体全文。
- 工作目录：识别残留构建/测试输出和已经提交的文件，明确生成物归属与清理
  边界。仅清理本轮确认可再生的产物，不删除源码、凭据、数据库、恢复点或
  他人未提交改动；最终 Git 状态与发布包须能逐项解释。

### 下一批可执行开发（按价值与风险排序）

| 批次 | 内容和边界 | 验收条件 | 状态 |
| --- | --- | --- | --- |
| N1 | 正文解析质量：为已有欧委会官方打印 PDF 适配器增加真实样本回放，并评估受控历史重试；不重复解析其 Angular HTML 壳或扩大版权范围 | 新样本正文可读，摘要保留；403/404/410 分类清晰且不高频重试；历史死信与新失败分开 | 待做 |
| N2 | 事实任务诊断：任务状态、extractor 版本、命中分母、受控重试和回填速率 | 后台能解释完成但无 Claim；失败重试有权限和审计；版本升级不无谓重跑全库 | 待做 |
| N3 | 天气 provider 用量和控制：持久请求计数、每日预算、分通道轮询设置 | 重启不重置额度；429 仅降级相关通道；关闭/未配置状态明确，后台不返回凭据 | 待做 |
| N4 | 事件质量与通知契约：固定窗口人工标注集，首发/升级/更正/冲突/转载分离 | 报告误合并和漏合并分母；不同事件不误并；重复支持不重发，重大新事实仍可通知 | 待做 |
| N5 | 受限领域事实更新及日报快照：明确更正保留旧证据，冻结 Claim/Evidence | 二手不覆盖一手；旧日报不读取实时事实；事实转换与 outbox 同事务、同幂等键 | 待做 |
| N6 | QWeather 小时预报条件性兜底：主预报陈旧/无效时启用，不常驻双抓 | 保存源时间/单位/来源；不平均概率，不跨产品重复日报或雨雪变化通知；请求受 N3 预算约束 | 待做 |
| N7 | 异机加密备份与完整恢复演练 | 先由维护者确定外部保存位置和访问授权；验证离机恢复及完整服务 RPO/RTO，同盘备份不冒充灾难恢复 | 待条件确认 |

后续天气降水生命周期、AQI/高温/UV/能见度提醒必须有时间戳、阈值、滞回与
独立去重；模型估计不能称官方预警。多地点、准确率学习、第二通知渠道和
小模型只在前述边界稳定且有实际需求时推进。不因日志审计引入 PostgreSQL、
消息队列、第二套聚类系统或默认打开已明确关闭的外部 heartbeat/设备动作。

## 天气轨道（贯穿七个阶段）

天气不是第八套独立工程，而是沿用七阶段的可靠性、来源、日报、管理和运维
边界：

| 关联阶段 | 天气交付 | 状态 |
| --- | --- | --- |
| R1 | 日报迟到补发、可重复投递和 provider 故障状态 | 已部署；三路 QWeather 独立状态已接入 |
| R2 | Open-Meteo/QWeather 失败降级、数据新鲜度和可解释错误 | 已部署；小时互证/备用尚未启用 |
| E1 | 天气预报变化、雨雪 episode 和官方预警生命周期 | 已部署；完整降水生命周期仍是后续项 |
| S1 | 提供商适配器、JWT、有界请求和来源归属隔离 | 已部署；持久请求计数、每日预算和 provider quota 面板待做 |
| D1 | 日报天气快照、农历节日、空气质量和正午太阳角度 | 已部署 |
| U1 | 响应式天气页面、地图选点/搜索/浏览器定位、保存后刷新和通知开关 | 已部署；多地点待做 |
| O1 | 天气通道故障注入、恢复通知、部署后真实轮询和回滚证据 | 已部署并验收；完整跨主机灾难恢复另行演练 |

天气升级的执行顺序固定为：先完成 R1/O1 的低成本可靠性缺口，再评估 R2/S1
的请求预算与有界小时兜底，最后才考虑 AQI/UV 等新增预警、多地点和准确率
学习；AQI/UV 显示已有，不重复列为未实现。每次升级都必须
通过天气局部测试、相邻服务测试、完整门禁和真实生产只读验收；不以增加请求
次数或字段数量本身作为完成标准。

## 阶段 1：可靠性收紧（最高优先级）

### 1.1 日报发布状态机

后台目标显示：待生成、算法版已发布、AI 重试中、AI 已补发、重试结束和需
人工处理。由已有日报版本与 retry 状态推导，避免多份持久状态互相矛盾。

要求：

- 构建阶段和保存前都做确定性 `cluster_key` 去重；重复项合并证据，不丢弃
  一手、二手或社交报道。
- 错误按阶段分类：本地结构校验和证据读取在预留 AI 预算前完成；模型返回
  非法格式可在下一次生成改善，应允许重试。不得把所有 ValueError 当成永久错误。
- 每次尝试记录 provider、model、prompt 版本、错误类别、开始和结束时间。
- 算法版发布必须先于 AI 重试，重启不能丢失状态，重复执行必须幂等。

验收：同一批重复事件只产生一个日报项；结构性错误不会每分钟重复刷日志；
模型不可用时算法版仍然发布；成功重试只新增一个不可变 AI 版本。

### 1.2 正文抓取降级

保留现有正文级别和抓取任务状态，在读模型说明“有摘要/有全文”和“受限/解析
失败/待重试”；失败原因与正文可用程度分开。403 或解析失败时保留标题、摘要
和原文链接，并评估域名退避，不让全文失败阻断观察入库、事件聚类或日报。

验收：正文抓取失败的文章仍可在事件页和日报中阅读；重复 403 不会形成
高频重试；来源健康页能区分 RSS 成功与正文失败。

## 阶段 2：事件质量

### 2.1 聚类可解释性

保留 `Observation -> EventReport -> Event -> Digest/Alert` 四层关系。一手、
二手、社交报道在同一事件下平级保存，不覆盖原始事实。聚类必须使用实体、
主题、地区、时间和数字一致性，不能因泛化标题单独合并。

增加质量指标：单来源事件比例、重复标题比例、平均独立来源数、误合并和
未合并样本、冲突报道数量。为管理员提供合并、拆分、标记主事件和查看匹配
理由的审计入口。

### 2.2 事件生命周期

事件自动从 `active` 进入 `quiet`、`closed`，迟到的高匹配报道可以重新打开。
事件详情提供时间线、来源层级、冲突事实和通知记录。日报和日常事件页只
读取事件池，不维护第二套聚类结果。

## 阶段 3：信源和外部服务可插拔

复用现有 Collector/ProviderRegistry 端口，独立负责获取、游标、标准化、
错误分类和正文策略；审计任何仍需核心认识具体网站的缺口，不另建同义接口。

来源质量采用长期窗口和最低样本数：短期网络抖动只影响健康状态，长期低质量
才降低权重，人工覆盖必须审计。AI 和通知同样只通过 provider/transport 端口
接入，Prompt 采用版本化注册表。
现有质量默认至少 30 个有效样本、跨度 90 天、90 天半衰期，符合用户的数月
观察要求；不因为这次路线图改成短期 30 天调权。

验收：新增一个 RSS、官方列表或 API 来源只需实现适配器和目录条目；停用一个
来源不需要修改日报、事件或通知核心逻辑。

## 阶段 4：日报和管理端

- 日报数量继续由信息密度、重要度、新鲜度、来源独立性和主题覆盖动态决定，
  50 条只是硬上限，不是目标数量。
- AI 负责证据内归纳和表达，算法负责候选选择、通知路由和事实边界。
- 管理端增加日报尝试历史、来源诊断、事件聚类检查、权重解释、AI provider
  故障转移、Prompt 版本和通知渠道管理。
- 日常事件页面支持时间窗、重要度排序、来源层级、冲突状态、已读/关注和
  手动加入日报。

## 阶段 5：运维、测试和扩展

- 为 RSS/HTML 建立历史快照回放测试，为聚类建立属性测试和人工标注样本。
- 对模型超时、403、DNS 失败、ntfy 不可用、数据库锁和进程重启做故障注入。
- CI 必须执行迁移检查、完整测试、构建、部署后健康检查和回滚验证。
- 保持 SQLite 隔离、WAL、备份和恢复演练。只有在多实例并发写入、锁竞争或
  查询规模达到瓶颈时才迁移 PostgreSQL；队列先继续使用数据库持久任务表，
  通过 `JobQueue` 端口保留未来替换空间。

## 每个阶段的交付规则

每个实现 PR 必须同步更新受影响专题文档、`CHANGELOG.md`、测试和回滚说明。
生产部署前确认版本、schema、配置 revision、服务健康、来源状态、outbox 和
一条受控测试链路。未能完成的项目必须写明当前状态、阻塞原因、下一步和验收
条件，不能只留在聊天记录中。
